// basic operations are: search, traversal,insert, delete
#include <iostream>
#include "template.h"
#include "Iterator.h"

T class BST
{
    struct node
    {
        type key;
        node *left;
        node *right;
        node();
        ~node();
    };
};